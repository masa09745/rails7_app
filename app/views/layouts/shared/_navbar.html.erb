<nav class="navbar navbar-dark bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">Rails7App</a>
    <div class="d-grid gap-2 d-md-flex justify-content-md-end">
      <% if user_signed_in? %>
        <%= button_to( "マイページ", root_path, class: "btn btn-dark" ) %>
        <%= button_to( "ログアウト", destroy_user_session_path, method: :delete, class: "btn btn-dark" ) %>
      <% else %>
        <%= button_to( "ログイン", new_user_session_path, method: :get, class: "btn btn-dark" )%>
        <%= button_to( "新規登録", new_user_registration_path, method: :get, class: "btn btn-dark" )%>
      <% end %>
    </div>
  </div>
</nav>